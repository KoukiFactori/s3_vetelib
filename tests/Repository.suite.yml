actor: RepositoryTester
suite_namespace: App\Tests\Repository
modules:
    # enable helpers as array
    enabled:
        - Symfony:
            app_path: 'src'
            environment: 'test'
        - Asserts:
        - Doctrine2:
            depends: Symfony
            cleanup: true
